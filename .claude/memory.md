# memory.md — Persistent Project Memory

> Claude reads this file to recall past decisions, design choices, and context
> across sessions. Append new entries at the bottom with a date.

---

## 2026-06-21 — Fixed: video stories/posts showed blank on iPhone Pi Browser

- **Trigger**: user tested the new Stories feature on an actual iPhone in Pi Browser and reported the story viewer opened (✕/Delete buttons visible) but the photo/video itself never appeared — just a black box.
- **Root cause**: iOS Safari/WebKit (which Pi Browser uses on iPhone) has a long-standing bug where `<video src="data:video/...;base64,...">` simply won't load — see `.claude/rules/tech-defaults.md`. Images as data URIs are unaffected; only video. This was a real bug, not just stories-specific — the same pattern existed in the regular journal feed's video posts (`js/journal.js`, `js/main.js`) and would have silently failed there too on iPhone.
- **Fix**: added `dataUrlToObjectUrl()` to `js/common.js` — converts the stored data URI to a `Blob` + `URL.createObjectURL()` at render time, only for `<video>` elements (images keep using the data URI directly). Applied in three places: the story viewer, the story tray thumbnails, and both feed renderers' video post cards (`buildUnifiedPostCard` in `main.js`, `buildPostCard` in `journal.js`). Also added `error` listeners on story media so a genuine load failure shows a toast instead of staying silently blank.
- **Why this didn't surface earlier**: WebFetch/desktop-browser testing can't catch this — Chrome/Firefox/desktop Safari all play data-URI video fine; it's iOS WebKit specifically. Worth remembering for any future media feature: test video playback on an actual Pi Browser/iPhone, not just desktop.

## 2026-06-21 — Boot steps isolated; close button bug traced to a cascading throw

- **Trigger**: after the video data-URI fix, user still saw a blank story AND the viewer's ✕ close button stopped working entirely — clicking it did nothing.
- **Root cause**: `dataUrlToObjectUrl()` (new in the previous fix) threw on the corrupted stored video's malformed base64 (`atob()` throws on invalid input). That throw happened inside `renderUnifiedFeed()`/`renderStories()`, which ran earlier in the same un-isolated `DOMContentLoaded` callback as `initStoryViewer()` — an uncaught throw stops the rest of that synchronous callback, so `initStoryViewer()` (which wires the ✕/Delete listeners) never ran. One corrupted post's media silently broke an unrelated button.
- **Fix**: (1) `dataUrlToObjectUrl()` now returns `null` instead of throwing — every call site checks for `null` and shows a "corrupted, can't be played" message instead of assuming success. (2) Added `runBootStep(fn)` to `js/common.js`, wrapping every top-level call in both `main.js`'s and `journal.js`'s `DOMContentLoaded` handlers — each step now runs in its own try/catch so one failing step can never prevent later ones from running.
- **Lesson**: any `DOMContentLoaded` handler with multiple sequential calls is a hidden single point of failure — the first uncaught throw silently cancels everything after it, with no console-visible symptom obvious to an end user (just "stuff stopped working"). Always isolate boot steps once a page has more than one independent feature initializing in the same handler.

## 2026-06-18 — Project Initialized

- **Concept**: Track personal balance across 5 life dimensions, mapped to the Five Elements (Wu Xing): Metal=Money, Wood=Health, Water=Talent/Skills, Fire=Mood, Earth=Situation/Circumstances.
- **Stack decision**: Pure HTML/CSS/JS, no frameworks, no build step — required by the target deployment platform, not just a simplicity preference.
- **Platform**: Pi Network App Studio — the app must run inside the Pi Browser and load the Pi SDK (`https://sdk.minepi.com/pi-sdk.js`), calling `Pi.init({ version: "2.0", sandbox: true })`. Stays in `sandbox: true` until the user explicitly says otherwise.
- **Starter color palette** (initial pick, not yet user-confirmed — revisit before treating these as final):
  - Metal `#b8860b` (dark goldenrod — money/wealth association)
  - Wood `#4caf50` (green — health/growth)
  - Water `#2196f3` (blue — calm/flow)
  - Fire `#e64a3c` (red-orange — mood/energy)
  - Earth `#8d6e4f` (brown/tan — grounded/stable)
- **Folder scaffold**: `.claude/` structure (rules/, agents/, skills/, settings.json, settings.local.json, memory.md) was scaffolded to mirror the sibling PTE project's framework, with generic/project-specific placeholder content rather than a literal copy of PTE's PTE-exam-specific content.
- **Carried-over lesson from PTE**: if/when real data files are added under `data/`, use the `window.<NAME>_DATA` script-loaded convention (not `fetch()` + raw `.json`) — Chromium blocks `fetch()` of local files when opened via `file://`, and a top-level `const`/`let` in a classic `<script>` does NOT attach to `window`, so the assignment must be `window.X = [...]` explicitly. See PTE's `.claude/memory.md` (2026-06-18 entries) for the full incident if this needs re-explaining later.

## 2026-06-18 — Dark theme + marketing-style landing sections

- **Trigger**: user shared screenshots of Resend's marketing site (resend.com) and asked to rebuild the landing page in that visual style ("follow 1-2-3" = hero → trust-logo-style row → feature/integration row).
- **Decision**: explicitly confirmed override of the light starter palette from the previous entry with a dark theme (`--bg-page: #0a0a0a`, `--bg-card: #141414`, light text). The 5 element colors were brightened (not just inverted) so they stay legible on near-black. This **resolves** the "not yet user-confirmed" flag on the original palette — dark-mode values are now the real tokens; see `.claude/rules/design.md`.
- **Typography addition**: added `'Fraunces'` (serif) as a *display-only* font for hero/section headlines, layered on top of Inter (still the body font). Approved in `.claude/rules/tech-defaults.md`.
- **New `--accent-gradient`** token (Fire → Metal) used sparingly for one highlighted word per hero/section headline, mirroring the gradient-highlight treatment in the reference screenshots.
- **New sections added to `index.html`** (all built with existing vanilla HTML/CSS/JS — no new dependencies beyond the one font):
  1. Hero — badge, serif headline, subtitle, two CTAs, and a CSS-only "orb + 5 orbiting element nodes" graphic (no images/assets used).
  2. Elements showcase — a trust-logo-row equivalent listing the 5 elements as hoverable badges (hover reveals each element's own color), instead of literal company logos since this isn't a B2B product.
  3. "How it works" — adapted from Resend's dev-integration section into a 3-step explanation (Rate / Reflect / Rebalance) plus a static visual preview panel (tabs + bars) — intentionally non-interactive, since there's no real tracking data wired up yet (see Open TODOs).
- **Heading hierarchy fix**: hero title is now the page's single `h1`; "Today's Balance" demoted to `h2`; the 5 element-card names demoted to `h3` (previously 5 sibling `h2`s with no `h1` on the page).
- Existing dashboard element cards were restyled (not restructured) to use the new dark tokens + `-tint` rgba hover backgrounds instead of the old flat pastel `-light` hex values.

## 2026-06-18 — Per-element "journal" (social-style posting), local-only demo stage

- **Trigger**: user asked for clicking an element card to open a page where they can post text/photo/video/audio and like posts, "similar to TikTok/Facebook." That's a real multi-user social-network ask, which conflicts with this project's hard "static files only, no backend" deployment constraint (Pi App Studio).
- **Resolved scope** (explicitly chosen by the user over the alternatives): build a **personal local journal per element** — single user, no accounts, nothing shared between people or devices. User's own words on the backend question: "get a demo testnet first, I will add it later" — i.e. ship the client-only version now, revisit a real backend before any production/Pi submission.
- **What was built**:
  - `journal.html` — one shared page (not 5 near-duplicate files), themed at runtime from a `?el=metal|wood|water|fire|earth` query param via `js/journal.js`.
  - Composer: text + a single file input (`accept="image/*,video/*,audio/*"`), one attachment per post, detected by MIME type.
  - Feed: newest-first, like/unlike (heart toggle + count), delete. All per-element, persisted to `localStorage` — see `.claude/rules/tech-defaults.md` ("User-Generated Content" section) for the storage key convention and the 4MB attachment guard against `localStorage`'s quota.
  - Extracted `showToast` / `initPiSdk` out of `js/main.js` into a new shared `js/common.js`, since `journal.html` needed the same two helpers — avoids duplicating them a second time.
  - Index.html element cards are now real `<a href="journal.html?el=...">` links (previously `<div>`s that just popped a "coming soon" toast).
- **Known limitation to revisit later** (per the user's "I will add it later"): no cross-device sync, no real accounts, no media hosting — everything lives in one browser's `localStorage`. Swapping this for a real backend is a deliberate future step, not an oversight.

## 2026-06-20 — Growth chart concepts (5 combo bar+line designs), comparison stage

- **Trigger**: user asked for a chart combining bar + line graphs showing growth of the Five Elements based on journal post counts, and wanted 5 different designs to compare before picking one.
- **What was built**: standalone `chart-concepts.html` + `js/chartConcepts.js`, not wired into the dashboard yet — purely a side-by-side comparison page (linked from nowhere in the nav; open the file directly).
  1. Grouped bars (per-element weekly counts) + single bold trend line for the combined weekly total.
  2. Stacked bars (weekly composition across elements) + 5 thin overlaid lines tracing each element's cumulative growth on a shared secondary scale.
  3. Small-multiples — one mini bar+sparkline combo per element, styled to drop into the existing `element-grid` card layout.
  4. Ranking bars (all-time total per element, sorted desc) + a dual-axis growth-rate line (recent vs. earlier weeks, zero baseline).
  5. Hero-style full-width "glow line" timeline (soft background bars + 5 glowing cumulative-growth lines + end-value labels), matching the dark marketing aesthetic already used in `index.html`'s hero/how-it-works sections.
- **Data source**: reads real posts straight from the same `localStorage` keys `journal.js` already uses (`lifebalance_journal_<element>`), bucketed into the last 8 weeks by `createdAt`. Falls back to a fixed demo dataset (clearly labeled via an on-page banner) when there are zero posts across all elements, so the comparison page isn't empty before any real journaling has happened.
- **No new dependencies**: all 5 charts are hand-built inline SVG (`document.createElementNS`), no chart library — `tech-defaults.md` requires CDN JS libs to be approved individually and none were, so this was the constraint-compliant path.
- **Next step (not yet decided)**: user picks a winning design, then it gets wired permanently into `index.html`'s dashboard (or wherever they want it) using real per-element totals instead of the demo fallback.

## 2026-06-20 — "How it works" preview bars now show real post counts

- **Trigger**: user wanted the Today/This Week/This Month bars in the landing page's "How it works" section (previously static mockup percentages) to count and display the real number of journal posts per element.
- **What changed**: `js/main.js` now reads the same `localStorage` keys as `journal.js`/`chartConcepts.js` (`lifebalance_journal_<element>`), counts posts per element within the selected range, and renders each bar's width relative to the largest count (plus the raw number at the end of the bar). Tabs were converted from `<span>` to real `<button>` elements with `data-range="today|week|month"` so they're keyboard-operable and wired to re-render on click.
- **Range definitions** (rolling windows, not calendar week/month, to avoid timezone/week-start ambiguity): Today = since local midnight, This Week = last 7 days, This Month = last 30 days.
- If there are zero posts everywhere, all bars correctly render at 0 width with "0" counts — no demo-data fallback here (unlike `chart-concepts.html`), since this lives on the marketing page and an all-zero state is an honest, normal first-run state.

## 2026-06-21 — Pi Developer Portal setup: hosting, domain validation, privacy policy

- **Hosting**: pushed the project to GitHub (`github.com/tokkcoin/Minh-Nhat-HA`, `main` branch) and deployed it on **Vercel** at `https://minh-nhat-ha.vercel.app` (auto-deploys on push). GitHub Pages was tried first but rejected by Pi's portal — Pi's Frontend URL field requires a bare root domain with no path, and GitHub Pages project sites are `username.github.io/repo-name/` (a path). The `username.github.io` special-repo route was blocked by a pre-existing repo of that name on the account, so Vercel was used instead since its default subdomains (`project.vercel.app`) have no path.
- **Domain validation**: `validation-key.txt` (containing the exact key the portal gave) lives at the repo root and is served at `/validation-key.txt` — this is how Pi's "Validate Domain Ownership" checklist step was satisfied.
- **Privacy policy**: added `privacy-policy.html`, live at `https://minh-nhat-ha.vercel.app/privacy-policy.html`. Content is accurate to the actual code as of this date — local-only storage, no accounts/analytics/tracking, lists Google Fonts + Pi SDK as the only third-party requests. **Must be revisited and updated if the data-handling story changes** (e.g. the payments backend below, or any future real backend) — don't let this page go stale.
- **Pi app identity confirmed**: the Pi Developer Portal entry for this app is named **"Mind map"** internally (differs from the site's own "Life Balance" branding — just a naming choice, same app) and is registered on **Mainnet**, not testnet.

## 2026-06-21 — Pi U2A payment verification (Mainnet, real Pi)

- **Trigger**: the Developer Portal's final checklist item, "Process a Transaction on the App," requires one real User-to-App (U2A) payment processed through the app at its production URL. (The user initially described this as "app-to-user to 5 wallets," but the actual checklist step — confirmed via screenshot — is a single U2A payment. No 5-wallet A2U requirement actually exists in this checklist.)
- **Mainnet risk explicitly confirmed by the user**: this app is on Mainnet, so this is a real (small) Pi payment, not test currency. User was asked directly and confirmed they understand and want to proceed anyway.
- **Why this needed a backend exception**: Pi's Payments API (`/v2/payments/{id}/approve` and `/complete`) requires the app's secret Server API Key, which can never be shipped client-side. This is the first genuine, deliberate exception to the project's "static files only, no backend" rule — see `.claude/rules/tech-defaults.md` ("Pi Payments Backend" section) for the exact scope of the exception (two Vercel serverless functions, nothing more).
- **What was built**:
  - `api/approve-payment.js`, `api/complete-payment.js` — Vercel zero-config Node serverless functions, read `PI_API_KEY` from a Vercel environment variable (never committed to the repo, never pasted into chat).
  - `pi-test-payment.html` + `js/piPayment.js` — a standalone page (not linked from main nav, `<meta name="robots" content="noindex">`) with a clear Mainnet/real-money warning banner, an "authenticate → createPayment → approve → complete" flow for a fixed 0.01 π amount, and an `onIncompletePaymentFound` handler per Pi SDK requirements.
- **Not done yet / still needs the user**: add `PI_API_KEY` to Vercel's environment variables (the user must do this themselves in the Vercel dashboard — not something that can be done from here), then open `pi-test-payment.html` inside Pi Browser at the Mainnet production URL and actually tap pay once to complete the checklist step.

## 2026-06-21 — "Sign in with Pi" (username scope)

- **What was built**: `js/piAuth.js` (auto-triggers on `index.html` load, plus a manual "Sign in with Pi" button in the header) and `api/verify-auth.js` (validates the client's `accessToken` against Pi's `GET /v2/me`, then issues an HMAC-signed HttpOnly session cookie — no database). `js/common.js`'s `initPiSdk()` was made async/memoized so `Pi.init()` is awaited exactly once before any `Pi.authenticate()` call.
- **New required env var**: `SESSION_SECRET` (any long random string) — same pattern as `PI_API_KEY`, must be added in Vercel before sign-in will work, otherwise `/api/verify-auth` returns 500.
- **Not built**: sign-out, persisting users anywhere beyond the session cookie, using the session for anything yet (no protected routes/features check it). This is sign-in only, not a full account system.

## 2026-06-21 — Unified Facebook-style feed (replaces the 5-card dashboard)

- **Trigger**: user shared Facebook screenshots and asked for: a single combined feed (composer at top, all elements' posts together) instead of 5 separate cards/pages; a horizontal Stories-style row of the 5 elements that **filters the same feed in place** when clicked (no page navigation); and a mandatory 4-quadrant priority tag on every post (Emergency+Important / Emergency+Unimportant / Important+Not-Emergency / Unimportant+Not-Emergency — the Eisenhower matrix).
- **Decisions confirmed with the user** before building: (1) this replaces `index.html`'s dashboard section directly, not a new page; (2) element click = in-place filter, not navigation; (3) both element and priority level are required to post, no untagged posts.
- **What changed**:
  - `index.html`'s old `.element-grid` (5 `<a>` cards linking to `journal.html?el=...`) is gone, replaced by: a composer (text + attach + element `<select>` + priority `<select>`), a `.stories-row` of circular element icons (`role="tablist"`, click = `activeFeedFilter` + re-render, "All" included), and one `.feed` showing every element's posts merged and sorted by `createdAt` descending.
  - **Refactor**: pulled `ELEMENTS`, `PRIORITY_LEVELS`, `MAX_MEDIA_BYTES`, `journalStorageKey`/`loadElementPosts`/`saveElementPosts`, `timeAgo`, `escapeHtml`, `readFileAsDataUrl` out of `journal.js` into `common.js`, since `main.js`'s new unified feed needed the exact same post shape/storage and duplicating it again would've been the third copy. `chartConcepts.js`'s independent `ELEMENTS` array was renamed to `ELEMENTS_LIST` (now `Object.values(ELEMENTS)`) specifically to avoid a **duplicate top-level `const ELEMENTS` declaration** between it and `common.js` — both load on `chart-concepts.html`, and two `const ELEMENTS` in the same global scope would have been a hard `SyntaxError` crashing that whole page. Caught and fixed in the same pass as this change; watch for this class of bug whenever a new shared global is added to `common.js`.
  - `journal.html`/`journal.js` (the per-element pages) gained the same required priority-level `<select>` and now show a level badge too, so posts created there match the same shape the unified feed expects. These pages still work standalone but are no longer linked from `index.html`'s dashboard — reachable only by direct/bookmarked URL now.
  - Post objects now carry `element` and `level` fields explicitly (previously `element` was only implicit via which `localStorage` key a post lived under). Old posts created before this change won't have either field — `main.js` backfills `element` from the storage key when merging, and renders no level badge at all when `post.level` is missing, rather than showing a broken/blank badge.
  - Removed the now-dead `.element-grid`/`.element-card*` CSS (nothing references those classes anymore).
- **Not done**: no "sign out"/account tie-in to posts (posts are still anonymous "You", unrelated to the new Pi sign-in feature above — these two features don't talk to each other yet).

## 2026-06-21 — "Create a Story" (Facebook-style), separate from the element filter row

- **Trigger**: user asked for a Facebook-style "create a story" feature. The existing `.stories-row` (All/Metal/Wood/Water/Fire/Earth chips at the top of the unified feed) already borrowed Stories' visual language but was repurposed as a feed *filter*, not actual stories — needed to resolve that overlap before building.
- **Decisions confirmed with the user**: (1) stories are a separate "+ Create Story" bubble in a new tray above the existing filter row, not tied to any element, so the filter chips keep their current click-to-filter behavior unchanged; (2) no auto-expiry — a story stays until manually deleted, unlike real Instagram/Facebook's 24h expiry; (3) a story requires a photo or video (no text-only stories), with an optional caption.
- **What was built**: a new `.stories-tray` in `index.html` (the "+ Your Story" create bubble, plus a `#stories-list` container that JS fills with one circular thumbnail per existing story) and a full-screen `#story-viewer` overlay (close ✕, delete 🗑, click-the-backdrop-to-close). All logic lives in `js/main.js` (`loadStories`/`saveStories`/`renderStories`/`buildStoryChip`/`openStoryViewer`/`closeStoryViewer`/`deleteCurrentStory`/`initStoryCreate`/`initStoryViewer`).
- **Storage**: single key `lifebalance_stories` (not per-element, unlike journal posts) — `{ id, mediaType: 'image'|'video', mediaData, caption, createdAt }`. See `.claude/rules/tech-defaults.md`.
- **Reused rather than rebuilt**: the `.story-chip`/`.story-chip__icon` circle styling from the existing filter row (just added `--create`/`--user`/`--thumb` modifier classes), and the same `MAX_MEDIA_BYTES`/`readFileAsDataUrl`/`showToast` helpers from `common.js` already used by the journal composer.

## 2026-06-21 — Fixed: black screen covering the whole app (Pi Browser + desktop)

- **Trigger**: user reported the app showed a full black screen and was unusable, starting right after the "Create a Story" feature was added.
- **Root cause**: `.story-viewer` in `css/style.css` (the full-screen story-viewer overlay, `background: rgba(0,0,0,.9)`) is styled with `display: flex` directly on the class, with no matching `.story-viewer[hidden] { display: none; }` rule. Author stylesheet rules always beat the browser's built-in `[hidden] { display: none }` default regardless of selector specificity, so the overlay rendered full-screen and black on every page load even though `index.html` has `hidden` on the element — `hidden` was never actually doing anything.
- **Fix**: added `.story-viewer[hidden] { display: none; }` right after the `.story-viewer` rule in `css/style.css`.
- **Second factor (Pi Browser specifically)**: `css/style.css` was never cache-busted with a `?v=` query param the way `js/common.js`/`js/main.js`/`js/journal.js` already are (see the 2026-06-21 video-fix entry above — Pi Browser caches aggressively). Bumped to `css/style.css?v=4` in both `index.html` and `journal.html` so the fix actually reaches devices with a cached old CSS.
- **Lesson**: any full-screen/modal overlay toggled via the `hidden` attribute needs an explicit `[hidden] { display: none }` override the moment it also has a class rule setting `display`. And CSS needs the same cache-busting discipline as JS for Pi Browser — easy to forget since the JS fix only touched `?v=` on script tags last time.

## 2026-06-21 — CapCut-inspired Story Editor: element tags, filter/trim/layers, music picker

- **Trigger**: user asked to upgrade "Create a Story" so stories are tagged to one of the 5 elements like journal posts, get a CapCut-style editor (filter presets, video trim, draggable text/sticker layers), and a ~100-track royalty-free music picker pool.
- **Scope confirmed with the user before building** (see the plan at the time, `witty-pondering-wolf`): single media file only — no multi-clip splicing/merging ("cắt ghép" in the literal multi-clip sense was explicitly ruled out). Real NCS-style track URLs were not supplied yet; the music picker was built against an empty `data/storyMusic.js` extension point instead of fabricated/placeholder URLs.
- **Key architecture decision — live composite, not re-encoded export**: rather than `canvas.captureStream()` + `MediaRecorder` + Web Audio mixing to bake a new video file (technically possible with zero new dependencies, but MediaRecorder support on Pi Browser's mobile WebKit is unverified — the same risk class as the iOS `<video src="data:...">` bug fixed earlier today), stories keep their **original, untouched `mediaData`**. Trim/filter/layers/music are stored as small JSON metadata on the story object and applied **live, every time the story is viewed** — `openStoryViewer()` in `js/main.js` seeks/loops the video between `trimStart`/`trimEnd`, applies `filterKey` via the native CSS `filter` property, renders `layers` as absolutely-positioned DOM elements on top of the media, and plays `musicTrackId` through a separate synced `<audio>`. This satisfies every named feature as real, working, user-controlled behavior without the export-file risk; it cannot produce a single downloadable/shareable video, but nothing in scope needed that.
- **What was built, in 3 shipped phases** (see the plan file for full rationale per phase):
  1. **Element tagging + minimal editor shell**: stories now require an element (`#story-editor-element`, same validation toast pattern as the post composer). Built the full `#story-editor` overlay shell once so later phases just added panels into it. Also merged stories into the main unified feed (`loadAllPostsMerged()`/`buildStoryFeedCard()` in `main.js`) so they're filterable by element/All alongside journal posts, with a click on the media opening the existing full-screen `#story-viewer`.
  2. **Filters + trim timeline + draggable layers**: `js/storyEditor.js` added `FILTER_PRESETS` (CSS `filter` strings, exported via `window.FILTER_PRESETS` since top-level `const` doesn't auto-attach to `window` — see the `tech-defaults.md` `window.X_DATA` note), a two-handle Pointer-Events trim scrubber (`initTrimTimeline`/`applyTrimLoop`), and draggable text/sticker layers (`addTextLayer`/`addStickerLayer`/`makeLayerDraggable`, positioned in `xPct`/`yPct` so they're resolution-independent). `applyTrimLoop()` and a read-only `renderLayersReadOnly()` are shared between the editor's own preview and `openStoryViewer()` so this logic isn't written twice.
  3. **Music picker**: `data/storyMusic.js` ships `window.STORY_MUSIC_TRACKS = []` (empty, with the exact `{id, title, artist, url, durationSec}` shape documented in a comment) — paste real tracks in later, no other file changes needed. The picker panel shows a friendly "Chưa có nhạc" empty state rather than a blank panel. `openStoryViewer()` creates/syncs a looping `<audio>` element alongside the video/image, started in the same synchronous click-handler tick as the media (required for mobile autoplay-gesture policy).
- **Real bug caught during Phase 1 testing — silent localStorage quota failures**: `saveElementPosts()`/`saveStories()` called `localStorage.setItem()` directly with no error handling. Once a few video posts/stories accumulated, `setItem` started throwing `QuotaExceededError` (origin-wide quota is only ~5-10MB total, well below what a handful of 4MB video attachments add up to) — the throw happened inside an unguarded click handler, so the story/post silently failed to save while the UI still looked like it succeeded (editor closed, but nothing appeared in the feed). This presented to the user as "works sometimes, fails sometimes" with no visible cause. **Fix**: added `safeSetItem(key, value)` to `common.js` (try/catch around `setItem`, shows a toast on failure, returns a boolean), routed both save functions through it, and made every caller (`initUnifiedComposer`, `journal.js`'s post handler, `initStoryCreate`'s `onCommit`, `commitStoryFromEditor`) check the return value — on failure the editor/composer now stays open with the user's content intact instead of silently discarding it. **Lesson**: any `localStorage.setItem()` in this app must go through `safeSetItem`, not called directly — quota failures are a real, easy-to-hit condition once media attachments are involved, not just a theoretical edge case.
- **Not done yet**: real music tracks. Pi Browser/touch-device re-test of the new Pointer-Events trim/layer dragging and simultaneous video+music playback hasn't happened yet — only desktop-browser testing so far.

## 2026-06-21 — Music source switched from NCS to Pixabay Music; self-hosted files, not hotlinked

- **Trigger**: user asked to find/populate real NCS track links into `data/storyMusic.js`. Investigated NCS's actual distribution model before doing that (web search + fetching `ncs.io` and `ncs.io/usage-policy` directly).
- **Why NCS was dropped**: NCS's usage policy only documents permission to use their tracks as background music inside a creator's own video/livestream (YouTube/Twitch) with artist credit in the description — it does not address, and so does not clearly grant, rehosting the actual audio files as a built-in music-picker/library feature inside a third-party app (this app's actual use case). Their FAQ says project needs outside standard video embedding should fill out a commercial licensing form. Separately, NCS's own materials explicitly warn that ~40% of "NCS download" traffic goes to unofficial MP3 aggregator/converter sites, which carry malware/legal risk — so those were never a real option either.
- **Switched to Pixabay Music** instead: confirmed via their license-summary page that Pixabay-licensed content (including music) may be used for commercial purposes, copied, modified, and redistributed, with no attribution required — a clean fit for embedding inside an app, unlike NCS's video-specific policy.
- **Self-hosted, not hotlinked**: there's no Pixabay API key configured for this project, and hotlinking sourced URLs (from NCS, Pixabay, or anywhere) isn't something to fabricate or scrape per this assistant's own constraints. Instead, added `audio/` (with `audio/README.md` documenting the workflow) as a place for the user to manually download real Pixabay tracks and drop the files in; `data/storyMusic.js` entries then point to a relative local path (`audio/<file>.mp3`) instead of an external CDN URL. Updated `data/storyMusic.js`'s header comment to match.
- **Still open**: user needs to actually download tracks from pixabay.com/music and add entries — `STORY_MUSIC_TRACKS` is still empty. When real tracks are added, no `privacy-policy.html` update is needed for this *specific* path (self-hosted files = no new third-party network request), unlike the original CDN-URL plan.

## 2026-06-21 — First 9 real music tracks added to the picker

- User downloaded 9 royalty-free MP3s (filenames match Pixabay's "no copyright music" naming convention, e.g. `alexgrohl-no-copyright-music-bounce-on-it-184234.mp3`) into `audio/NCS/` (their own folder name — not literal NCS-brand files; the dropped-NCS decision above still stands) and asked to wire them in.
- `data/storyMusic.js`'s `STORY_MUSIC_TRACKS` now has 9 real entries, `id`/`title`/`artist` derived from each filename, `url` pointing at the self-hosted `audio/NCS/<file>.mp3` path. `durationSec` omitted (no `ffprobe`/`ffmpeg` available in this environment to measure real duration — left out rather than guessed, since the picker UI doesn't currently render it anyway).
- One duplicate file was found (`nastelbom-no-copyright-music-473269 (1).mp3`, identical byte size to the non-`(1)` copy, presumably an accidental double download) — only one entry was added for it; the duplicate file itself was left in place (not deleted) and is noted in `audio/README.md` for the user to clean up whenever convenient.
- Bumped `data/storyMusic.js?v=2` in `index.html`.

## 2026-06-21 — Media attachments moved from localStorage base64 to Cloudinary (free plan)

- **Trigger**: user asked how to increase storage capacity for the app's media (photos/videos), since `localStorage`'s shared ~5-10MB origin quota was forcing a hard 4MB-per-file guard (`MAX_MEDIA_BYTES`, see the Story Editor entry above for the `QuotaExceededError` incident this guard was already added for).
- **Options considered**: IndexedDB (still client-only, bigger quota but still single-device, no real fix); Vercel Blob (1GB free, Hobby tier is non-commercial-only which conflicts with the Pi payments feature already shipped); Supabase Storage (1GB free); Cloudinary (25GB free/month via a 25-credit pool covering storage+bandwidth+transforms, and free-plan per-file caps of 10MB image / 100MB video — by far the largest free ceiling and purpose-built for media). User picked Cloudinary.
- **What changed**:
  - New `api/cloudinary-sign.js` — a fourth Vercel serverless function (same pattern as the Pi payment/auth endpoints), issues a signed-upload signature using `CLOUDINARY_API_SECRET` so the secret never reaches the client. The actual file upload goes straight from the browser to Cloudinary's API, not through this function — keeps large videos off Vercel's request-body limit.
  - New `uploadMediaToCloudinary(file)` in `js/common.js`, replacing `readFileAsDataUrl()` (now deleted, no longer used anywhere) as the thing called when a post/story is actually submitted. Every call site (`journal.js` composer, `main.js` unified composer, `storyEditor.js`'s `commitStoryFromEditor`) disables its Post button and toasts "Uploading..." during the call, and shows an error toast + re-enables the button on failure rather than silently losing the post.
  - `MAX_MEDIA_BYTES` (was a flat 4MB) replaced by `MAX_IMAGE_BYTES` (10MB) / `MAX_VIDEO_BYTES` (100MB) + a `maxBytesForFile(file)` helper in `common.js`, matching Cloudinary's actual free-plan per-file caps instead of `localStorage`'s old quota.
  - `js/storyEditor.js`'s editor now previews the raw `File` via a plain `URL.createObjectURL(file)` instead of reading it into base64 up front — the file is only uploaded to Cloudinary at commit time (when the user taps Post), so cancelled/abandoned edits cost zero uploads. `commitStoryFromEditor` is now async and captures all needed `storyEditorState` fields into local variables *before* the upload `await`, since `closeStoryEditor()` (which can fire if the user closes the editor mid-upload) nulls out `storyEditorState`.
  - `dataUrlToObjectUrl()` in `common.js` now passes through any string that isn't a `data:` URI unchanged — old posts/stories made before this change still have base64 data URIs and keep working via the existing conversion path, while new Cloudinary `https://res.cloudinary.com/...` URLs (which never had the iOS data-URI bug to begin with) go straight through.
- **Not done yet / still needs the user**: sign up for a free Cloudinary account and add `CLOUDINARY_CLOUD_NAME`, `CLOUDINARY_API_KEY`, `CLOUDINARY_API_SECRET` as Vercel Environment Variables (same place `PI_API_KEY`/`SESSION_SECRET` live) — uploads will fail with a 500 from `/api/cloudinary-sign` until this is done. No migration of existing base64 posts to Cloudinary was done (old posts keep working as-is via the data-URI fallback).

## 2026-06-26 — Finance Rebalance engine (Metal/Money 4-pool allocator), tested and a layout bug fixed

- **What this is**: `js/financeRebalance.js` + a new `#finance` section in `index.html` (between Elements Showcase and How It Works). Lets the user enter a total capital figure, split it across 4 fixed pools (Đầu tư/Invest — Pi Network, Tiết kiệm/Savings, Phát triển bản thân/Self-dev, Khẩn cấp/Emergency) by a user-editable %-allocation formula (must sum to 100), and tracks the Invest pool's live gain/loss against the real Pi Network price (CoinGecko's public `simple/price` endpoint, no API key). When any pool's return crosses ±5% it shows a toast warning and a "Cân bằng lại" (rebalance now) button that re-splits the *current* (post-gain/loss) total back across the formula.
- **Math is plain JS, no LLM in the loop** — `computePoolViews()` is the only place values are derived; this is a deliberate constraint per the file's own header comment, since an AI advisory layer is planned on top later and must read this engine's output rather than recompute/guess numbers itself.
- **Storage**: two new localStorage keys, `lifebalance_finance_config` (the %-formula) and `lifebalance_finance_state` (capital totals + each pool's principal + the Invest pool's `priceAtBuy` snapshot), both routed through `common.js`'s `safeSetItem` per the existing quota-safety convention (see the 2026-06-21 Story Editor entry above).
- **Bug found and fixed during this session's testing**: the new `.finance-pools` responsive grid overrides (2-col at ≤900px, 1-col at ≤420px) were added inside the *existing* `/* 16. Responsive */` media-query block, but the base `.finance-pools { grid-template-columns: repeat(4,1fr); }` rule was appended in a new section *after* it — so the unconditional base rule, being later in source order at equal specificity, always won regardless of viewport width. Verified visually at 768px/420px with Playwright screenshots: all 4 cards stayed in one row/overflowed. **Fix**: moved the whole Finance Rebalance CSS block to before the Responsive section (renumbered Finance → 16, Responsive → 17, keeping the file's existing convention of Responsive always being the last section) so the media queries now correctly come after the base rule in source order. **Lesson**: this file's component sections must stay before the Responsive section, not after — appending a new component block past it silently breaks any responsive override that component needs.
- **Tested via Playwright** (chromium-cli wasn't available in this environment; used a scratch Playwright script against `file://` instead, since this is a static no-build-step app): 100,000,000đ total → correctly split 30/30/30/10M; config-sum validation correctly rejects a non-100 sum and shows the Vietnamese hint text; state persists correctly across a full page reload; no console errors from `financeRebalance.js` (only a pre-existing, unrelated Pi SDK `postMessage` origin warning that also fires with no finance code present). Not yet tested: the ≥5% rebalance-warning/button path itself (requires the live Pi price to actually move 5%+ from `priceAtBuy`, not easily simulated without mocking the CoinGecko fetch) and real Pi Browser/mobile testing (this app already has a backlog item for that — see Open TODOs).
- **Not done / open questions for the user**: `monthlySalary`/`income`/`expense` inputs are captured and persisted in `finance-state` but are not currently fed into the total-capital math anywhere (the user still manually types the total each time) — unclear if that's intentional (manual entry preferred) or a planned next step; don't assume either way without asking. No UI yet surfaces `monthlySalary`/`income`/`expense` back out anywhere (write-only fields right now).

## 2026-06-26 — Hero orb nodes become real navigation; each element gets its own dedicated page

- **Trigger**: user asked for two things on the hero's 5 orbiting element icons (previously purely decorative, `aria-hidden`, static positions): (1) make them bigger and visually "alive," with continuous circular motion; (2) clicking one should open a dedicated page to keep building that element's features in — Metal→finance management, Wood→health, Fire→mood/emotions, Water→skills, Earth→tactical situation/position.
- **Decision confirmed with the user** (asked directly, since it materially changes file count/structure): dedicated page per element, same-tab navigation (not an anchor-scroll within `index.html`, not `target="_blank"`) — matches this project's existing multi-page convention (`journal.html`, `chart-concepts.html`, etc.) rather than inventing a new pattern.
- **What was built**:
  - The 5 `.hero-orb__node` decorative `<div>`s became real `<a href="…">` elements (`finance.html`/`health.html`/`skills.html`/`mood.html`/`situation.html`), each with an `aria-label` (e.g. "Metal — Money") since they're emoji-only link text. `aria-hidden="true"` moved off the whole `.hero-graphic` wrapper onto just the inner `.hero-orb` backdrop circle (still purely decorative), since the nodes are now real interactive navigation and must stay in the accessibility tree.
  - New `.hero-orbit` wrapper (full-size absolute overlay inside `.hero-graphic`) spins continuously via `hero-orbit-spin` (26s linear infinite); each node runs its own `hero-orbit-counter-spin` (same 26s/linear/infinite, opposite direction, combined with a `scale(1.1)` mid-animation bump in the *same* keyframe so it doesn't fight the orbit-spin transform) so the emoji stays upright while still appearing to orbit, plus a separate `hero-node-pulse` animation on `box-shadow` (a different CSS property, so it can run concurrently without clobbering the transform animation) using each element's existing `--*-tint` color as `--node-glow`. Node size went 56px→76px, font-size 22px→34px, border 1px→2px.
  - **Deliberate, explicit exception to `design.md`'s "no animations longer than 1.5s except the progress ring" rule** — these are intentionally infinite/continuous decorative animations, requested directly by the user for this specific element. Don't generalize this exception to other components without the same kind of explicit ask.
  - **Metal's page (`finance.html`)**: the `#finance` section built in the previous session (see the entry above) was *moved out* of `index.html` into its own file, keeping the exact same markup/IDs/CSS classes — only the surrounding page shell changed (now uses the `journal.html`-style simple header + `.journal-back`/`.journal-hero` per-element header pattern, reusing those existing classes rather than inventing new ones). `js/financeRebalance.js` now owns its own `DOMContentLoaded`/`runBootStep` boot block directly (previously called from `main.js`), matching how `journal.js` self-boots — `main.js` no longer references finance at all, and the `<script src="js/financeRebalance.js">` tag moved from `index.html` to `finance.html`.
  - **Wood/Fire/Water/Earth pages** (`health.html`, `mood.html`, `skills.html`, `situation.html`): scaffolds only, no real features yet — same page shell as `finance.html`, a themed `.journal-hero` (icon + title + dimension, using `--current-element`/`--current-element-tint` inline via `style=""` since each page is permanently one element, unlike `journal.html`'s URL-param-driven version), and a new generic `.element-placeholder` block (badge + one line of Vietnamese explaining what's coming) — reused across all 4 rather than 4 separate one-off styles. Each has its own near-empty `js/{health,mood,skills,situation}.js` (just `runBootStep(initPiSdk)` for now) as the intended landing spot for that element's real features later, matching the one-page-one-JS-file convention already used everywhere else in this project.
  - Cache-busted `css/style.css` → `?v=9` and `js/main.js` → `?v=11` (both changed); new files start at `?v=1`.
- **CSS section renumbering**: inserted a new "17. Element Placeholder Pages" section, pushing the file's "Responsive" section from 16→18 — kept Responsive as the file's last section per the existing convention (and the bug fixed earlier this session, where a component section placed *after* Responsive silently broke that component's media-query overrides).
- **Tested via Playwright** (same `file://` + scratch-script approach as the Finance testing earlier this session — chromium-cli still not available in this environment): confirmed both orbit animations are actually applied (`getComputedStyle().animationName`), node size is 76×76px, all 5 `href`s/`aria-label`s are correct, clicking through to all 5 pages loads each with the right title/hero text and zero console errors, and `finance.html` still works fully standalone (100M đ → 30/30/30/10M split) after being moved out of `index.html`.
- **Not done**: the 4 new element pages are intentionally empty shells — no actual health/mood/skill/situation tracking features exist yet. Real Pi Browser/touch-device testing of the new orbit animation hasn't happened (same standing gap as other animation/touch features in this project).

## 2026-06-26 — Finance page polish: Home button, compact input bar, Enter-to-apply, "Thực tế" comparison panel

- **Trigger**: 4 follow-up requests after testing `finance.html`: (1) restyle the back-link into a "Home" button with a bigger arrow and white text; (2) shrink the total-capital/salary/income/expense input bar to roughly the same compact size as the allocation-formula bar below it; (3) let pressing Enter in those fields apply the formula instead of requiring a click on "Áp dụng vào 4 quỹ"; (4) add a second 4-card panel (same categories: Đầu tư/Tiết kiệm/Phát triển bản thân/Khẩn cấp) where the user manually enters actual figures to compare against the formula-driven panel above — each card shows only the total, with sub-items added/removed through an icon-opened editor, to keep the main view uncluttered.
- **What changed**:
  - `.journal-back` (shared by all 6 per-element pages, including the pre-existing `journal.html`) is now a pill-shaped button: white (`--text-primary`) bold text, card background, border, and a separately-sized `.journal-back__arrow` span (20px vs. 14px body text) so the arrow reads as a clear icon rather than an inline character. Only the 5 new pages' markup got the `<span class="journal-back__arrow">` treatment and "Home" label text — `journal.html`'s own link still says "← Back to dashboard" (wasn't part of this ask) but inherits the same button look via the shared class.
  - `.finance-inputs`/`.finance-field` went from a boxy card (labels stacked above inputs, ~87px tall) to a single compact row (labels inline before inputs, ~46px tall) — closely matching `.finance-config`'s (~59px) compactness, same general approach (small padding, no box-shadow, smaller font).
  - `financeRebalance.js`: `initFinanceRebalance()` now adds a `keydown` listener for `Enter` on all 4 of `finance-total-capital`/`finance-salary`/`finance-income`/`finance-expense` that calls `handleApplyTotal()` — the existing click-the-button path was *kept*, not removed (Enter is now the fast path, the button remains for non-keyboard/accessibility use; the user's phrasing implied replacing the button as the primary interaction, not literally deleting it).
  - **New "Thực tế" (actual) panel**: a second `localStorage` key `lifebalance_finance_actual` (`{ invest: [], savings: [], selfDev: [], emergency: [] }`, each an array of `{id, label, amount}` sub-items) — deliberately separate from `lifebalance_finance_state`, since this panel is a manually-maintained comparison, not derived from the formula at all. New `#finance-actual-pools` grid reuses the exact same `.finance-pools`/`.finance-pool` markup/CSS as the target panel (just a ✏️ edit button added to the card head) so the two panels look like a matched pair. Clicking ✏️ opens `#finance-subitem-modal` (a new lightweight centered-overlay component, `.finance-modal`, distinct from the heavier full-screen `.story-editor`/`.story-viewer` overlays — reused their `[hidden]{display:none}` pattern from the black-screen bug fixed earlier this project, but otherwise a new, simpler component since this only needs a small data-entry list, not a full-screen editor) listing that pool's sub-items with delete buttons and an add-row (label + amount + Enter-to-add, matching the same Enter-key convenience as the main form). The card itself only ever renders the **sum** of its sub-items plus a "N danh mục con" count — never the individual line items — per the explicit "keep the outside view clean" ask.
  - Cache-busted: `css/style.css` → `?v=10` (now referenced by all 6 pages that load it, including the unrelated `journal.html` which is still on `?v=4` — not touched, since its CSS dependency wasn't part of this change... actually since `.journal-back` IS shared, `journal.html` *will* pick up the new button style once its own cache-bust catches up; left as-is since this is desktop/file:// testing only so far, not yet a deployed cache-staleness concern).
- **Tested via Playwright** (same approach as earlier in this session): Home button renders with the arrow+text; Enter on the total-capital field correctly applies 200,000,000đ → 60/60/60/20M (30/30/30/10%); the two bars are visually comparable in height; opening the modal, adding two sub-items (via both Enter-key and button-click paths), and deleting one all update the card's total correctly (8,000,000đ → 3,000,000đ after delete); confirmed at 420px mobile width both panels stack cleanly; confirmed `journal.html` (pre-existing page, shares the `.journal-back` class) still renders fine with its own unchanged text. Zero console errors throughout.
- **Note on a misleading screenshot during testing**: a Playwright `fullPage: true` screenshot of `finance.html` appeared to show the sticky `.site-header` rendering *below* the hero/back-link instead of above — this was purely a known Playwright full-page-screenshot stitching artifact with `position: sticky` elements, not a real layout bug. Confirmed via a normal (non-fullPage) viewport screenshot that the actual DOM/visual order is correct. Worth remembering if a future full-page screenshot on this site looks like header/sticky elements are out of order — re-check with a plain viewport screenshot before assuming a CSS bug.

## 2026-06-26 — "Thực tế" sub-items get date + edit; two pie charts added

- **Trigger**: two more follow-ups on `finance.html`'s new "Thực tế" panel: (1) each sub-item needs a date field in addition to name/amount, and needs to be editable (not just deletable); (2) add two pie charts at the very bottom showing the breakdown of the target panel and the actual panel, by current total capital.
- **Sub-item model extended**: `{id, label, amount}` → `{id, label, amount, date}` (`date` is an ISO `yyyy-mm-dd` string from a native `<input type="date">`, defaults to today if left blank when adding). Old sub-items saved before this change simply have no `date` — `formatFinanceDate()` returns `''` for a missing/invalid date rather than crashing, so they keep displaying fine.
- **Edit reuses the add-row instead of a second form**: clicking ✏️ on an item (`startEditSubitem()`) pre-fills the label/amount/date inputs with that item's values, flips `finance-modal-add-btn`'s label to "Lưu", and tracks `financeModalEditingItemId`. The same `handleSaveSubitem()` either pushes a new item or replaces the matching one by id, depending on whether that variable is set — avoided building a separate edit modal/form for a one-field difference in behavior. Deleting the item currently being edited also resets the form back to "add" mode so it can't get stuck pointing at a now-gone id.
- **Two pie charts** (`#finance-chart-target-pie`/`#finance-chart-actual-pie` + matching `*-legend` lists), built with plain CSS `conic-gradient` on a circular div — no SVG arc math, no chart library (same constraint as `chart-concepts.html`'s hand-rolled charts). `buildConicGradient()` takes `{label, value, color}` slices and computes cumulative `%` stops; an empty/zero-total panel renders a flat neutral circle + "Chưa có dữ liệu" instead of a broken gradient. Both pies share one `FINANCE_POOL_COLORS` map (`invest→metal, savings→water, selfDev→wood, emergency→fire`) — this is also now applied to the pool cards' border-top/bar-fill colors (previously only `invest` had a real color; the other 3 fell back to neutral gray), so a slice's color means the same pool everywhere on the page, cards and charts alike. `renderFinanceCharts()` is called from the tail of both `renderFinanceDashboard()` (target side) and `renderFinanceActualPools()` (actual side), so any action that changes either panel's numbers — applying the formula, rebalancing, the periodic price refresh, or adding/editing/deleting a sub-item — keeps both charts in sync automatically.
- **Tested via Playwright**: confirmed the target pie's conic-gradient stops and legend percentages (100M đ → 30/30/30/10%); confirmed the actual pie starts in the empty state and updates correctly after adding a dated sub-item; confirmed editing an existing sub-item updates it in place (item count stays the same, amount/date change, pie/legend reflect the new total) rather than creating a duplicate; confirmed the modal's new 2-row layout (label on its own row, amount+date+button below) doesn't overflow horizontally even at 375px viewport width with a long category name. Zero console errors.

## 2026-06-26 — Grand total added to each pie chart panel

- **Trigger**: "thêm thông tin tổng số tiền cho mỗi bảng tương ứng" — show the grand total for each of the two panels, not just the per-category breakdown the legend already had.
- **What changed**: `renderFinancePieChart()` now takes a third DOM-id argument and writes `Tổng: <formatVnd(total)>` into a new `<p id="finance-chart-target-total">`/`<p id="finance-chart-actual-total">` sitting between each chart's title and its pie circle. Same total the pie/legend already derive from (sum of the 4 slices), so it can't drift out of sync — no separate calculation. Tested: 100.000.000đ target total and a 25.000.000đ actual total (one savings sub-item) both displayed and updated live.

## 2026-06-26 — Wood/Health page built: "Kỷ luật thép" daily discipline tracker

> **Superseded 2026-06-28** — the user found this too heavy/"metal-like" and asked for a simpler quest system instead (see the entry below). The 4-criteria target/streak data described here was deleted, not migrated. Kept this entry for the historical reasoning, but don't treat anything below as the current state of `health.html`/`health.js`.

- **Trigger**: user asked (after first saying "Thổ" then correcting to "Mộc" — Wood is the Health element, Earth is Situation) to turn the `health.html` placeholder into a real "iron discipline" page: 4 fixed criteria (Ăn/Ngủ/Thể dục/Nghỉ ngơi) each with a daily target (e.g. sleep 7h/day) vs. actual, plus a free-form daily task checklist that can be added to and edited.
- **Decisions confirmed with the user before building**: (1) the 4 criteria track via numeric target + daily logged value (not a simple yes/no checkbox) — chosen specifically so it could mirror `finance.html`'s already-proven Target/Actual pattern (% achieved, history, edit); (2) "Tập thể dục"'s target unit is "lần/ngày" (sessions/times per day), resolving the ambiguous "10" in the user's original example.
- **What was built** (`js/health.js`, replacing the placeholder boot-only file; `health.html`'s `.element-placeholder` section replaced):
  - **4 criteria cards** (`HEALTH_CRITERIA`: eat/sleep/exercise/rest, colored via the existing 5-element palette — eat=wood, sleep=water, exercise=fire, rest=earth — reusing tokens rather than inventing new ones, per `design.md`). Each card has a **fast inline "Hôm nay: [số] [Lưu]" path** (writes/overwrites today's entry directly, one click, no modal — added specifically because requiring a modal open for a once-a-day log would add daily friction this kind of habit tracker can't afford) plus a ✏️ icon opening a history modal (same `.finance-modal` CSS classes reused verbatim from `finance.html` — zero new modal CSS needed, consistent with this project's precedent of reusing `.journal-back`/`.journal-hero` across unrelated pages) where the target can be changed and past dated entries can be added/edited/deleted (mirrors the finance sub-item modal's "add-row doubles as edit-row" pattern exactly).
  - **Streak math**: walks backward day-by-day from today (or from yesterday if today has no entry yet, so an not-yet-logged today doesn't show a falsely-broken streak) counting consecutive days where the logged value met that day's *current* target. Verified this correctly reacts to a target change — raising sleep's target from 7→8 immediately flipped a previously-met 7.5 entry to "Chưa đạt mục tiêu" with the streak badge disappearing, since streak/met are always derived live from the current target, never snapshotted.
  - **Daily task checklist**: separate from the 4 fixed criteria — `lifebalance_health_tasks` (the task list itself: `{id, text, createdAt}`) + `lifebalance_health_task_log` (`{taskId: [dates the task was checked done]}`), so checking a task off only marks *today*, and the checkbox naturally unchecks again the next day without any extra "reset" logic — it just reflects whether today's date is in that task's log array. Add/edit reuse one input + button (button label flips "Thêm"↔"Lưu"), same pattern as the criteria modal and as `financeRebalance.js`'s sub-item form.
  - **"Kỷ luật thép" visual treatment**: a bold uppercase `.health-banner` (wood-green outline + tint, letter-spaced badge text "⚔️ KỶ LUẬT THÉP", motivational tagline) — deliberately stayed inside the existing token system (no new hardcoded colors) per `design.md`; the "discipline" feel comes from typography/boldness/streak-badges rather than a new color scheme.
  - New storage keys: `lifebalance_health_config`, `lifebalance_health_logs`, `lifebalance_health_tasks`, `lifebalance_health_task_log` — all through `safeSetItem` per the established convention.
  - CSS renumbered: inserted "17. Health Discipline Tracker" before the existing "Element Placeholder Pages" (now 18) and "Responsive" (now 19) sections — kept Responsive last per the convention fixed earlier this project (a component section placed after Responsive silently breaks its own media queries).
- **Tested via Playwright**: 4 cards render with correct targets/units; quick-log writes today's entry and shows correct %/streak/status; history modal's target-change and backfill-entry-with-past-date both work and correctly recompute the card; task add/toggle/edit/delete all work, edit reuses the same form correctly (no duplicate created); responsive at 1024/768/420px (2-col → 1-col, matching `.finance-pools`' existing breakpoints). Zero console errors.
- **Not done**: `mood.html` (Fire), `skills.html` (Water), `situation.html` (Earth) are still empty placeholder scaffolds — only Wood/Health and Metal/Finance have real features so far.

## 2026-06-28 — Wood/Health page rebuilt again: replaced "Kỷ luật thép" with a wuxia-MMO quest system

- **Trigger**: user found the just-built target/streak/history-modal tracker too heavy/complex ("không cần giống mục metal" — don't need it to look like the Finance page) and asked for something simpler and faster to operate instead: a quest system styled after wuxia MMOs ("game võ lâm"), with Nhiệm vụ chính/phụ/hàng ngày/hàng tuần/hàng tháng (Main/Side/Daily/Weekly/Monthly quests).
- **Decisions confirmed with the user before rebuilding**: (1) the previous 4 fixed criteria (eat/sleep/exercise/rest) and all their target/log data are dropped entirely, not migrated — "bỏ hẳn, bắt đầu trắng"; (2) the quest system has real XP + level mechanics (not just a plain checklist), since the user explicitly wants game-like progression.
- **What was built** (`js/health.js` rewritten again, `health.html`'s criteria/banner/modal sections replaced):
  - **5 quest categories** (`QUEST_CATEGORIES`): main (🏆, default 100 XP), side (⚔️, 30 XP), daily (📅, 10 XP), weekly (🗓️, 50 XP), monthly (📆, 200 XP) — defaults auto-fill into the XP input when the category `<select>` changes, but stay user-editable per quest.
  - **One shared add/edit row** at the top (title + category + XP + button) instead of 5 separate per-category forms — deliberately the *only* composer on the page, in service of "đơn giản hoá... thao tác nhanh". Editing reuses the same row (button flips "Thêm"↔"Lưu"), same pattern used throughout this project's other CRUD UIs (`financeRebalance.js`'s sub-items, the previous health task checklist).
  - **Recurrence via period-keys, not a separate "reset" job**: each quest stores `completedPeriods: []`, a list of period-keys it's been completed for. A quest's "done right now" checkbox state is just `completedPeriods.includes(currentPeriodKey(category))` — daily uses today's local date, weekly uses that week's Monday's date, monthly uses `YYYY-MM`, and main/side (one-time, non-recurring) use a constant `'once'` key. This means nothing has to actively "reset" a daily quest at midnight — it naturally reads as not-done the moment the date rolls over, because yesterday's key is no longer the current one.
  - **XP/Level is always derived, never stored as a running counter**: `computeTotalXp()` sums `completedPeriods.length × xp` across all quests every render. Unchecking a quest immediately and correctly subtracts its XP back (verified: complete a 500XP main quest + a 10XP daily → 510 total; uncheck daily → back to 500) — this was a deliberate choice over incrementing/decrementing a separate stored XP total, specifically to avoid drift/double-counting bugs and to make deleting a completed quest correctly shrink the total too (verified: deleting the completed main quest dropped the level/XP bar back down immediately, no leftover phantom XP). Level = `floor(totalXp / 100) + 1`; crossing into a new level shows a `🎉 Lên cấp N!` toast.
  - **Real bug caught and fixed during this build — UTC vs. local date for day/week boundaries**: `todayKey()`/`weekKey()` initially used `date.toISOString().slice(0,10)`, which is always UTC. For a Vietnam-timezone (UTC+7) user, that rolls a "day" over at 7am local time instead of local midnight — a daily quest checked off late at night could appear to un-reset 7 hours early, or a quest done right after midnight could still count toward "yesterday." **Fix**: added `formatLocalDate()` (built from `getFullYear()`/`getMonth()`/`getDate()`, the local-time accessors, not the UTC `toISOString()` one) and routed both key functions through it. Same UTC-fallback bug existed in `financeRebalance.js`'s `handleSaveSubitem()` (the "default to today if no date picked" fallback) — fixed the same way, since that file has no shared date-formatting helper to reuse, inlined the same local-parts logic there. **Lesson**: any future "what's today's date" logic in this app must use local date parts, never `toISOString()` — that one is a recurring trap specifically because it looks correct and works fine for UTC/UTC-adjacent testers.
  - **Legacy data migration**: `migrateLegacyHealthData()` runs once (guarded by checking whether `lifebalance_health_quests` already exists) — folds the *previous* free-form daily task checklist (`lifebalance_health_tasks`/`_task_log`, from the session before "Kỷ luật thép") forward into 'daily' quests with their existing per-day completion history intact, then deletes all four now-dead legacy keys (`_config`/`_logs`/`_tasks`/`_task_log`). The "start blank" decision applied only to the target/streak tracker's own data, not the separate task-checklist feature that came before it — preserved that one rather than silently discarding real user-entered data.
  - Reused `.health-task`/`.health-task__check`/`.health-task__text`/`.health-task--done` CSS as-is (no changes) for quest rows, and `.finance-pool__edit-btn`/`.finance-modal__item-delete` for the edit/delete icons — continuing this project's established precedent (`.journal-back`/`.journal-hero` reused across unrelated pages) of reusing existing generic-enough classes rather than duplicating near-identical CSS under feature-specific names.
  - Removed all now-dead CSS from the previous build: `.health-banner*`, `.habit-cards`/`.habit-card*`, `.habit-streak`, `.health-modal__target-row`/`.health-modal__unit`, `.health-tasks__add-row` (including their responsive-breakpoint references).
- **Tested via Playwright**: all 5 groups render with correct icons/labels; XP auto-fills per category and is overridable; completing/unchecking quests adds/removes XP correctly including the level-up toast; editing a quest updates in place (no duplicate); deleting a quest correctly shrinks the XP total; the legacy-migration path was tested by seeding the old keys, reloading, and confirming the old keys are gone and the migrated quest appears with its prior completion date intact; no horizontal overflow at 375px mobile width. Zero console errors.

## 2026-06-28 — Quests can now carry an image (Cloudinary, same as posts/stories)

- **Trigger**: "Xây dựng tính năng có thể build và phát hành nhiệm vụ có thể tạo thủ công bằng và dùng hình ảnh" — quests should be manually creatable with an attached image.
- **Decision confirmed with the user**: image storage goes through Cloudinary (the same `uploadMediaToCloudinary()` helper in `common.js` already used by posts/stories), not a small base64 thumbnail in `localStorage`. Trade-off made explicit to the user before building: this means quest images only actually upload on the deployed Vercel site — opening `health.html` directly via `file://` (as has been done for all testing this session) cannot reach `/api/cloudinary-sign`, since that's a serverless function with nothing to serve it locally. Same limitation every other media feature in this app already has.
- **What was built**: the quest add/edit row gained a 📷 button (hidden file input behind it, `accept="image/*"`) + a small thumbnail preview + a ✕ to remove it. Picking a file just shows an instant local preview via `URL.createObjectURL()` — nothing uploads yet. The actual Cloudinary upload only happens inside `handleSaveQuest()` at the moment "Thêm"/"Lưu" is pressed (mirrors `journal.js`'s composer pattern exactly: disable the button, toast "Đang tải ảnh lên...", `await uploadMediaToCloudinary()`, re-enable and bail with an error toast on failure *without* saving the quest — so a failed upload never leaves a half-saved quest or a silently-dropped image). Editing a quest that already has an image pre-fills the preview from its stored `imageUrl`; the ✕ button sets a `removeExistingQuestImage` flag distinct from "no change," since "user picked nothing new" and "user explicitly wants the image gone" need different handling on save. Quest rows show the thumbnail (`.quest-thumb`, 28×28px) next to the title when `imageUrl` is set.
- **Tested via Playwright** (everything reachable without the live Cloudinary endpoint): file picked → preview shows instantly; oversized file rejected via the existing `maxBytesForFile()` check; attempting to save with a pending image against the unreachable local `/api/cloudinary-sign` fails gracefully — toast shown, button re-enabled, **no quest created** (confirmed count stayed 0); removing the pending image and saving without one still works normally; editing a quest seeded with a real Cloudinary URL correctly shows that image in both its row thumbnail and the edit-preview. The actual upload-success path (a real Cloudinary round-trip) still needs verifying on the deployed site or in Pi Browser — added to Open TODOs.

## 2026-06-28 — Live period rollover (no reload) + 50 starter quests seed button

- **Trigger**: two asks: (1) confirm/ensure Main/Side quests stay completed forever while Daily/Weekly/Monthly reset "theo thời gian thực" (in real time); (2) generate a starter set of quests — 10 per category — covering health/eating/sports/rest/sleep, for the user to edit afterward.
- **(1) was already correct by design** (main/side use a constant `'once'` period-key, so nothing ever un-checks them; daily/weekly/monthly recompute "done" from the *current* period key on every render) — but "theo thời gian thực" read as wanting the UI to actually flip live while a tab stays open across midnight, not just whenever the page next happens to reload. Added `checkPeriodRollover()` on a 30s `setInterval`: compares today/this-week/this-month's keys against the last-seen ones (`lastSeenPeriodKeys`, initialized right after the first render so the very first tick can't false-positive) and calls `renderQuests()` only when one of them actually changed. Verified by monkey-patching `Date` inside the page to jump one day forward and calling the check function directly — a previously-checked daily quest correctly un-checked itself with no reload.
- **(2) `SEED_QUESTS`**: a 50-entry array (title/category/xp), 10 each for main/side/daily/weekly/monthly, themed across all 5 requested areas (sức khoẻ/ăn uống/thể thao/nghỉ ngơi/giấc ngủ) mixed within each category rather than one theme per category. Wired to a new "🌱 Tạo 50 nhiệm vụ mẫu" button (`handleSeedQuests()`) that **appends** to whatever quests already exist — never auto-runs, never overwrites — since the user said they'd edit the set themselves afterward and nothing in this app silently seeds/replaces user data without an explicit click (same precedent as the empty `STORY_MUSIC_TRACKS` left for manual population). Clicking it twice just appends 50 more (no dedupe) — acceptable since it's a one-shot manual action, not something expected to be clicked repeatedly.
- **Tested via Playwright**: seed button produces exactly 10 quests in each of the 5 groups (50 total); clicking again appends to 100; the rollover check correctly un-checks a daily quest the moment the simulated date crosses midnight, without any page reload. Zero console errors.

## 2026-06-28 — Bulk select-and-delete for quests

- **Trigger**: "thêm tính năng xoá nhiệm vụ để chỉnh sửa" — after seeding 50 starter quests, asked for a faster way to delete several at once while curating the list down. Per-quest 🗑 already existed; clarified via `AskUserQuestion` which bulk style was wanted — chose **checkbox-select-then-delete** (not a per-group "clear all" or a single page-wide "clear everything").
- **What was built**: each quest row gained a second checkbox (`.quest-select-check`, `data-quest-select`) separate from the completion checkbox — selecting doesn't touch XP/done state. A `selectedQuestIds` Set (module-level, persists across re-renders) drives a bar above the quest list (`#quest-bulk-bar`, hidden when nothing's selected) showing "N nhiệm vụ đã chọn" plus "🗑 Xoá đã chọn" and "Bỏ chọn" (clear selection without deleting). Selections can span multiple categories at once. `renderQuests()` prunes any selected id that no longer exists (e.g. the quest was deleted individually via its own 🗑 while also bulk-selected) so the count never lies. No `confirm()` dialog before deleting — matches this app's existing convention of immediate, undo-free deletes everywhere else (no `confirm()` calls anywhere in the codebase).
- **Tested via Playwright**: bar hidden by default; selecting 3 quests across 3 different categories shows the correct count; an unrelated completion-toggle (which re-renders the whole list) doesn't lose the selection; bulk delete removes exactly the selected ones (47 left of 50) and re-hides the bar; "Bỏ chọn" clears the selection without deleting anything; individually deleting a quest that was also bulk-selected correctly drops the count (2 → 1) instead of leaving a stale phantom selection. Zero console errors.

## 2026-06-28 — Water/Talent page built: icon-based skill badges (skills.html)

- **Trigger**: "phát triển tab thuỷ talent theo phong [cách] hiển thị Icon, có thể thêm tuỳ chỉnh, xoá Icon" — first real feature for the Water element page (was an empty placeholder scaffold until now). Confirmed via `AskUserQuestion`: icon source is a **curated emoji picker** (not Cloudinary image upload — keeps it fully offline/`file://`-testable, same simplicity reasoning as the quest groups' 🏆⚔️📅 icons), and each skill also tracks a **1-5 star proficiency level**, not just a name+icon.
- **What was built**: `lifebalance_skills` holds `{id, name, icon, level, createdAt}` objects. The add form (`#skill-add-form`, a real `<form>` so Enter-to-submit works natively) has a name input, a 24-emoji `SKILL_ICONS` picker grid (🎨💻🎸📚🏃♟️🎤🍳🧘🎮📷✍️🌐🔧🎬🧮🗣️🧗🚴🏊🎯🧩🥋🎲), and a 5-star level picker — both pickers are shared state (`selectedSkillIcon`/`selectedSkillLevel`) reused for add *and* edit, same "add-row doubles as edit-row" pattern as quests/finance sub-items (`skillEditingId`, button label flips "Thêm kỹ năng" → "Lưu"). Skills render as a CSS-grid of icon-centric cards (`.skill-grid`/`.skill-card`) — big circular icon, name, a row of 5 stars **clickable directly on the card** for quick in-place re-rating without opening edit, plus ✏️/🗑 actions. Empty state shows a placeholder message instead of an empty grid.
- **Tested via Playwright**: icon picker renders all 24 + 5-star picker; adding a skill (via button click and via Enter-in-textfield) produces the correct icon/name/star count; clicking a star directly on a rendered card re-rates it in place (5→3 confirmed) without going through the edit form; edit pre-fills name/icon/level and updates in place with no duplicate card; delete removes exactly one; deleting the last skill brings back the empty-state message. Confirmed no horizontal overflow at 375px (2-column grid). Zero console errors.
- **CLAUDE.md updated**: `skills.html`/`skills.js` description changed from "placeholder/scaffold" to the real feature description; `mood.html`/`situation.html` (Fire/Earth) remain the only true placeholders left.

## 2026-06-28 — Skill "folder": notes + uploaded images per skill icon

- **Trigger**: "khi đã tạo các icon kỹ năng có thể click vào để mở folder lưu các thông tin, ghi chú, hoặc upload file hình ảnh và được lưu trong icon đó" — clicking a skill's icon should open a per-skill notes/image store, like a little folder attached to that badge.
- **What was built**: each skill object gained `notes` (string) and `images` (array of `{id, url}`). `.skill-card__icon` is now clickable (and keyboard-activatable via Enter/Space, `role="button" tabindex="0"`) and opens `#skill-detail-modal` — reused the existing `.finance-modal`/`.finance-modal__inner`/`.finance-modal__close` classes from the Finance Rebalance sub-item modal rather than inventing new modal chrome. Inside: a notes `<textarea>` (saved on `blur` and again defensively on modal close, only writing to storage if the value actually changed) and an image gallery fed by the same Cloudinary upload pattern already used for quest images (`uploadMediaToCloudinary`, disable-button + "Đang tải ảnh lên..." toast + graceful failure toast, same `maxBytesForFile` size check). Each image has its own ✕ to remove it from the skill's array (not deleted from Cloudinary itself — no feature in this app deletes from Cloudinary, only unlinks locally). A small badge showing the image count appears on the card's icon (bottom-right corner) once a skill has at least one image, so a skill with saved content is visually distinguishable before opening it.
- **Tested via Playwright**: modal hidden by default; opening shows the correct icon+name title; a typed note survives close-then-reopen; uploading against the unreachable local `/api/cloudinary-sign` (this is `file://` testing, same limitation as quest images) fails gracefully — toast, button re-enabled, no phantom image, no badge. Mocked `uploadMediaToCloudinary` to verify the success path end-to-end: two uploads produce 2 images + a "2" badge on the card; deleting one drops both the gallery and the badge to 1. Zero console errors.

## 2026-06-28 — Earth/Situation page built: strategic formation board (situation.html)

- **Trigger**: "ở tab hoả hay xây dựng theo phong cách tư duy, dàn trận sắp xếp, thuyên về trí tuệ chiến lực" — but the content described (strategic thinking, battle-formation arrangement) matched the project's original definition of **Thổ/Earth** ("xây dựng vị thế chiến thuật"), not Hoả/Fire (already defined as mood/emotion since the hero-orbit entry). Flagged the mismatch via `AskUserQuestion` instead of silently building on the named-but-wrong page — same pattern as the earlier Mộc/Thổ mix-up in the "kỷ luật thép" session. User confirmed: build on **`situation.html`**.
- **Decisions confirmed with the user**: the "quân cờ" placed in the formation are the user's **current goals/priorities** (not fixed life-area categories), arranged into a **3-tier military structure** — 🔥 Tiền tuyến (urgent), ⚔️ Trung quân (in progress), 🏯 Hậu phương (reserve/long-term) — rather than a 2×2 Eisenhower urgent/important matrix.
- **What was built**: `lifebalance_situation_units` holds `{id, name, zone, createdAt}`. `FORMATION_ZONES` is an ordered array (frontline → middle → rear) rendered top-to-bottom as three bordered sections, each left-accented by a different color (frontline = `--danger` red, middle = `--earth`, rear = `--text-muted`) for quick visual triage. A unit moves between zones via small ▲/▼ buttons (disabled at the array's edges) rather than HTML5 drag-and-drop — deliberate: native drag-and-drop has unreliable touch support, and this app's target is Pi Browser/mobile, so simple buttons were chosen over a "true" drag-and-drop board feel. Add/edit reuses the now-standard shared-form pattern (`unitEditingId`, button label flips "Thêm" → "Lưu"), same as quests/skills/finance sub-items.
- **Tested via Playwright**: 3 zones render with correct titles/hints; adding via button-click and via Enter-in-textfield both work; moving a unit down two zones (frontline → middle → rear) correctly relocates it each time and the source zone shows the empty-state message; the down-button is disabled once a unit reaches the rear zone; edit pre-fills name+zone and updates in place without creating a duplicate; delete removes exactly one. Confirmed no horizontal overflow at 375px. Zero console errors.
- **CLAUDE.md updated**: `situation.html`/`situation.js` description changed from placeholder to the real feature; only `mood.html` (Fire) remains a true placeholder now.
- **Renamed same day**: zone labels changed from the military Tiền tuyến/Trung quân/Hậu phương to the classical strategy triad **🌤️ Thiên thời / 🏞️ Địa lợi / 🤝 Nhân hoà** (timing / terrain-advantage / human-harmony — the Mencius "天时地利人和" triad), a more fitting "trí tuệ chiến lược" framing. Only the `FORMATION_ZONES` array's `label`/`icon`/`hint` and the HTML `<select>` options changed — the internal `zone` keys (`frontline`/`middle`/`rear`) and the move-direction (▲/▼) mechanic were left untouched, so any units saved before the rename resolve correctly under their new labels with zero migration needed. Verified via Playwright by seeding a unit under the old `zone: 'frontline'` value and confirming it renders correctly under the new "🌤️ Thiên thời" heading after reload.

## Open TODOs

- [x] `PI_API_KEY` Vercel env var — confirmed working 2026-06-20/21. Note: the dashboard showed it saved with Production checked *before* it actually took effect; a plain "Redeploy" of the existing deployment didn't pick it up, but a fresh `git push` (new deployment from scratch) did. If this env-var pattern recurs, prefer triggering a brand-new deployment over trusting the Redeploy button.
- [ ] Confirm the one-time Mainnet verification payment actually completes successfully inside Pi Browser
- [ ] Decide what a single "balance entry" (mood/health/etc. *rating*, distinct from the journal posts) looks like before building the rating half of the dashboard
- [ ] Real backend/multi-user social platform for the journal — explicitly deferred by the user, not yet scheduled
- [ ] Decide whether historical entries eventually persist via Pi Network storage, a real database, or both
- [ ] Pick a winning design from `chart-concepts.html` and wire it permanently into `index.html` (still pending, separate from the "How it works" bars above)
- [ ] Revisit `privacy-policy.html` content if the data story changes (now that a payments backend exists, double check the policy still accurately describes what little server-side processing exists)
- [ ] Download real tracks from pixabay.com/music into `audio/` (see `audio/README.md`) and add matching entries to `data/storyMusic.js` (shape documented in that file's header comment) — NCS was ruled out, see the 2026-06-21 "Music source switched" entry above
- [ ] Re-test the Story Editor's trim-handle/layer dragging (Pointer Events) and simultaneous video+music playback on an actual Pi Browser/touch device — only desktop-browser tested so far
- [x] Cloudinary env vars — confirmed working 2026-06-21 (upload returns 200, file lands in Media Library). Two real gotchas hit along the way, worth remembering: (1) the account uses Role-Based Access Control — a freshly generated API Key/Secret pair has **no permissions by default** until a Role is explicitly assigned at creation (picked "Admin" / Product environment); a key created without a role fails uploads with `403 Request forbidden due to missing permissions (actions=["create"])`, not an auth/signature error, which is misleading. (2) Same "Redeploy doesn't pick up new env vars" gotcha as `PI_API_KEY` applied again — needed a fresh `git push` (used an empty commit since no code had changed) before the new key took effect.
- [x] Revisit `privacy-policy.html` — already updated in the same change as the Cloudinary integration (see entry above), discloses Cloudinary as a third-party media host.
- [ ] Finance Rebalance: decide whether `monthlySalary`/`income`/`expense` should feed into the total-capital math automatically, or stay manual-entry-only/display-only as they are now (see 2026-06-26 entry)
- [ ] Finance Rebalance: re-test the ≥5% rebalance-warning path and "Cân bằng lại" button against a real Pi Network price move (only the allocate/persist/validate paths were tested so far, not the warning-threshold path itself)
- [ ] Finance Rebalance + Story Editor: still need real Pi Browser/touch-device testing (carried over from the 2026-06-21 entry, now applies to this feature too)
- [ ] Build real features into `mood.html` (Fire) — the last remaining empty placeholder scaffold; all other 4 elements now have real features (see the 2026-06-26/28 entries)
- [ ] Re-test the new Earth/Situation formation board (`situation.html`) on a real Pi Browser/touch device — only desktop `file://` tested so far
- [ ] Re-test the new Water/Talent skill-badge page (`skills.html`) on a real Pi Browser/touch device — only desktop `file://` tested so far
- [ ] Verify the skill-folder image upload (notes/images modal) actually succeeds against the real Cloudinary endpoint on the deployed site — same as the quest-image TODO above, only the local-failure/graceful-abort + mocked-success paths could be tested via `file://`
- [ ] Re-test the new hero-orbit rotation animation on a real Pi Browser/touch device (desktop-only tested so far)
- [ ] Re-test the Wood/Health quest system (XP/level, daily-quest day-rollover at local midnight) on a real Pi Browser/touch device — only desktop `file://` tested so far
- [ ] Verify the quest image upload actually succeeds against the real Cloudinary endpoint on the deployed site (only the local-failure/graceful-abort path could be tested via `file://`)
