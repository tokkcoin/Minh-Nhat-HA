/* ============================================================
   Life Balance — mood.js
   Fire (Mood) element page. Placeholder boot only — real
   mood/emotion-tracking features get built here next.
   ============================================================ */

'use strict';

document.addEventListener('DOMContentLoaded', () => {
  runBootStep(initPiSdk);
});
